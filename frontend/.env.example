VITE_USE_REAL_API=false
VITE_API_URL=http://localhost:8000

# Public artifact URL — points to the R2 bucket serving latest/*.json.
# For local dev, first run `python scripts/pull_live_wire.py` to populate
# data/live_wire/ from R2, then either:
#   - Serve latest/ locally (e.g. `npx serve ../latest`)
#   - Or set this to your R2 public URL
VITE_DATA_BASE_URL=https://your-bucket.r2.dev
